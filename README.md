# Emoji Finder, built with Svelte

Search emojis by name and copy to clipboard by click.

Start dev server:

```bash
  npm install
  npm run dev
```

Start build:

```bash
  npm run build
  npm run start
```
